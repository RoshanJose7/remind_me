class Task {
  bool isCompleted;
  String subject;
  String description;
  DateTime deadLine;

  Task({
    required this.isCompleted,
    required this.subject,
    required this.deadLine,
    required this.description,
  });
}
